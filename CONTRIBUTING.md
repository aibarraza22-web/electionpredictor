# Contributing

Add source adapters with tests for parsing, revisions, duplicates and availability dates. Never introduce future knowledge into a vintage. Add a registry hypothesis before promoting a metric; show sequential evidence and reason. Run `pytest -q` before submitting.
