# Simulation

Official runs use ≥50,000 seeded Monte Carlo draws (100,000 where practical). A chamber-wide error creates correlated errors, then race noise is added. Output includes seat distributions, means, medians, intervals, control probabilities, tipping race and an explicit Senate tie-break assumption. Future Alaska/Maine adapters must execute full ranked-choice transfer rounds; Georgia must model runoff separately.
